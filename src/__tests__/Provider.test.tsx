import * as React from 'react'

it.todo('should render max 3 columns per row')
it.todo('should render the header')
it.todo('should render the snippet')
it.todo('should render the action icons')
it.todo('should be able to add more actions with props.actions')
it.todo(
  'props.actions should be an array of objects with shape: { title, icon, ...rest }',
)
it.todo('columnCount should update when props.columnCount changes')
it.todo('props.theme should set the initial theme if provided')
it.todo('props.onThemeChange should invoke whenever state.theme updates if')
it.todo('renderHeader should be able to overwrite the default header component')
it.todo(
  'renderSnippet should be able to overwrite the default snippet component',
)
it.todo(
  'renderActions should be able to overwrite the default actions component',
)
