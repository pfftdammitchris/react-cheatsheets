import * as React from 'react'

it('should render max 3 columns per row')
it('should render the header')
it('should render the snippet')
it('should render the action icons')
it('should be able to add more actions with props.actions')
it(
  'props.actions should be an array of objects with shape: { title, icon, ...rest }',
)
it('columnCount should update when props.columnCount changes')
it('props.theme should set the initial theme if provided')
it('props.onThemeChange should invoke whenever state.theme updates if')
it('renderHeader should be able to overwrite the default header component')
it('renderSnippet should be able to overwrite the default snippet component')
it('renderActions should be able to overwrite the default actions component')
